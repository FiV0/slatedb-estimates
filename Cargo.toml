[package]
name = "slatedb_estimates"
version = "0.1.0"
edition = "2024"

[dependencies]
slatedb = { git = "https://github.com/slatedb/slatedb", branch = "fix-1597-6", default-features = false }

[dev-dependencies]
tokio = { version = "1.47.1", features = ["macros", "rt-multi-thread"] }
